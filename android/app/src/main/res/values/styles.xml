<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- خلفية نافذة داكنة — تمنع الوميض الأبيض قبل رسم WebView -->
        <item name="android:windowBackground">@android:color/black</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- أسود بحواف بنفسجية — يظهر قبل WebView وعلى أندرويد < 12 -->
        <item name="android:background">@drawable/splash_vignette</item>
        <!-- أندرويد 12+ يرسم شاشة الإطلاق من هذه الخصائص — بدونها يرث الأبيض -->
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <!-- أيقونة شفافة تماماً — النظام يفرض رسم أيقونة على 12+ فنجعلها غير مرئية لتبقى الشاشة سوداء فقط -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_empty_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
